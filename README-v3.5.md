# LABYRUN v3.5

Frontend polish pass:

- Player world coordinate now lines up with the puppet/sprite feet, so collision matches what the player sees.
- Mobile virtual joystick uses a stronger radial dead-zone, cardinal-axis snap, and aggressive release/visibility resets to reduce drift.
- Every Food Court receives a deterministic unique wall colour palette.
- Winner screen now uses the same character portrait three times for a deliberately ridiculous 1980s photo-studio / Ken Burns victory composition.
- No Render/server change is required for v3.5.
